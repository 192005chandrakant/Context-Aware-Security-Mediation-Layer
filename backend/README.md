# CASML Backend

This is the backend service for the **CASML (Context-Aware Security Middleware Layer)** research prototype.

It is built with FastAPI, Pydantic v2, and SQLAlchemy.

## Installation

To install in editable mode:
```bash
pip install -e ".[dev]"
```
